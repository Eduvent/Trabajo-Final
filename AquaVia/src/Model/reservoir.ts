export class Reservoir {
    lat: number;
    lon: number;
    name: string;

    constructor(lat: number, lon: number, name: string) {
        this.lat = lat;
        this.lon = lon;
        this.name = name;
    }
}